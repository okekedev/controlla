<Window x:Class="AirControllaWindows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AirControlla for Windows" Height="400" Width="600"
        Background="#FF5080FF">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0"
                   Text="AirControlla"
                   FontSize="36"
                   FontWeight="Bold"
                   Foreground="White"
                   HorizontalAlignment="Center"
                   Margin="0,20,0,10"/>

        <!-- Status Icon -->
        <Ellipse Grid.Row="1"
                 x:Name="StatusIndicator"
                 Width="20"
                 Height="20"
                 Fill="Gray"
                 HorizontalAlignment="Center"
                 Margin="0,10"/>

        <!-- Status Text -->
        <StackPanel Grid.Row="2"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center">

            <TextBlock x:Name="StatusText"
                       Text="Not Started"
                       FontSize="20"
                       Foreground="White"
                       TextAlignment="Center"
                       Margin="0,0,0,20"/>

            <TextBlock x:Name="ConnectionText"
                       Text="No controller connected"
                       FontSize="14"
                       Foreground="#E0E0E0"
                       TextAlignment="Center"
                       Margin="0,0,0,10"/>

            <!-- Administrator Warning -->
            <Border x:Name="AdminWarning"
                    Background="#FF5722"
                    CornerRadius="5"
                    Padding="15"
                    Margin="0,20,0,0"
                    Visibility="Collapsed">
                <StackPanel>
                    <TextBlock Text="⚠️ Administrator Privileges Required"
                               FontWeight="Bold"
                               Foreground="White"
                               TextAlignment="Center"
                               Margin="0,0,0,5"/>
                    <TextBlock Text="Please restart this app as Administrator for input simulation to work."
                               Foreground="White"
                               TextWrapping="Wrap"
                               TextAlignment="Center"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- Start/Stop Button -->
        <Button Grid.Row="3"
                x:Name="ToggleButton"
                Content="Start Receiver"
                FontSize="18"
                FontWeight="Bold"
                Padding="20,10"
                Background="White"
                Foreground="#FF5080FF"
                BorderThickness="0"
                Click="ToggleButton_Click"
                Cursor="Hand">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="5"/>
                </Style>
            </Button.Resources>
        </Button>
    </Grid>
</Window>
